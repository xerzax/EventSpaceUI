@page "/adminReport"
@using EventSpaceUI.Client.Utilities
@inject IApiService _apiService
@layout AdminLayout

<div class="container p-5">


    <div class="row pt-5">
        <div class="dark-box ">
            <canvas id="myChart1" width="200" height="100"></canvas>
        </div>
    </div>
    <div class="row pt-5">
        <div class="dark-box ">
            <canvas id="myChart2" width="200" height="100"></canvas>
        </div>
    </div>


</div>
@* <script>
    var ctx = document.getElementById('myChart1').getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: @Html.Raw(Json.Serialize(Model.EventLabel)),
            datasets: [{
                label: 'Events and sale',
                data: @Html.Raw(Json.Serialize(Model.SaleData)),
                backgroundColor: ['rgba(255, 99, 132, 0.2)', '#D8E0BB',
                    '#86A3C3', 'red', 'blue', 'green', 'orange'],
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 1
            }]
        },
        options: {
            scales: {
                yAxes: [{
                    ticks: {
                        beginAtZero: true
                    }
                }]
            }
        }
    });
    var ctx = document.getElementById('myChart2').getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: @Html.Raw(Json.Serialize(Model.CategoryLabel)),
            datasets: [{
                label: 'Events and sale',
                data: @Html.Raw(Json.Serialize(Model.CategoryData)),
                backgroundColor: ['rgba(255, 99, 132, 0.2)', '#D8E0BB',
                    '#86A3C3'],
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 1
            }]
        },
        options: {
            scales: {
                yAxes: [{
                    ticks: {
                        beginAtZero: true
                    }
                }]
            }
        }
    });

</script>

 *@